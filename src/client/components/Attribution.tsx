// FR-MASCOT-5, in one place so the sentence cannot drift between the two layouts. It names the
// cats as Live2D's sample characters, never as Cekgu's own.
export function Attribution() {
  return (
    <footer className="mx-auto max-w-[880px] px-4 py-6 sm:px-8 type-caption text-ink-muted">
      Tororo and Hijiki are Live2D sample characters, used under the Live2D Free Material License Agreement, and are not
      Cekgu's own. Built with the Live2D Cubism SDK.
    </footer>
  )
}
